# 文档自动化部署流程指南

## 📋 概述

本项目已配置自动化部署流程。每次您修改或添加文档时，GitHub Actions会自动：
1. 构建静态网站
2. 部署到GitHub Pages

您无需手动运行任何命令！

---

## 🚀 快速开始

### 1️⃣ 编辑或添加文档

在 `docs/` 目录中编辑或创建 markdown 文件：

```
docs/
├── index.md                 # 首页
├── part1/
│   ├── chapter1.md         # 第一章
│   └── chapter2.md         # 第二章
└── part2/
    ├── chapter3.md         # 第三章
    └── chapter4.md         # 第四章
```

### 2️⃣ 更新导航配置（如需要）

编辑 `mkdocs.yml` 文件的 `nav` 部分，添加新章节：

```yaml
nav:
  - 简介: index.md
  - 第一部分:
    - 第一章: part1/chapter1.md
    - 第二章: part1/chapter2.md
    - 第三章: part1/chapter3.md    # 新增
  - 第二部分:
    - 第四章: part2/chapter4.md
```

### 3️⃣ 提交并推送到 main 分支

```powershell
git add docs/
git add mkdocs.yml
git commit -m "Add new chapter: chapter3"
git push origin main
```

### 4️⃣ 自动部署

- GitHub Actions 自动检测您的推送
- 自动构建文档网站
- 自动部署到 gh-pages 分支
- 几秒钟后，您的网站将被更新！

---

## 📝 常见操作

### 添加新章节

**步骤1：在 `docs/` 中创建新的 markdown 文件**

例如，创建 `docs/part1/chapter3.md`：

```markdown
# 第三章：标题

内容开始...

## 小节1

更多内容...
```

**步骤2：更新 `mkdocs.yml`**

```yaml
nav:
  - 第一部分:
    - 第一章: part1/chapter1.md
    - 第二章: part1/chapter2.md
    - 第三章: part1/chapter3.md  # ← 添加这一行
```

**步骤3：提交推送**

```powershell
git add docs/part1/chapter3.md mkdocs.yml
git commit -m "Add chapter3 to part1"
git push origin main
```

### 修改现有章节

只需编辑对应的 markdown 文件，然后：

```powershell
git add docs/part1/chapter1.md
git commit -m "Update chapter1 content"
git push origin main
```

### 修改网站主题或配置

编辑 `mkdocs.yml` 并推送：

```powershell
git add mkdocs.yml
git commit -m "Update theme settings"
git push origin main
```

---

## 🔍 监控部署进度

### 在 GitHub 网页上查看构建状态

1. 进入仓库：https://github.com/eriche2016/VFM_course
2. 点击 **Actions** 标签
3. 查看最新的工作流执行情况

### 工作流状态说明

- 🟢 **绿色勾号**：部署成功，网站已更新
- 🟡 **黄色圆圈**：正在构建中
- 🔴 **红色叉号**：构建失败，检查错误日志

---

## 📱 查看已发布的网站

部署完成后，访问您的网站：

**https://eriche2016.github.io/VFM_course/**

---

## ⚙️ 工作流配置文件

自动化脚本位置：`.github/workflows/deploy.yml`

**触发条件：**
- ✅ 推送到 `main` 分支时自动触发

**自动执行步骤：**
1. 检出代码
2. 安装 Python 3.10
3. 安装依赖：mkdocs、mkdocs-material
4. 运行 `mkdocs build` 生成静态网站
5. 部署到 gh-pages 分支

---

## 🛠️ 本地测试（可选）

如果想在本地测试效果再推送到GitHub：

```powershell
# 安装依赖
pip install mkdocs mkdocs-material

# 启动本地服务器
cd VFM-course
mkdocs serve

# 在浏览器打开 http://localhost:8000 预览
```

---

## 📖 支持的 Markdown 功能

使用 Material 主题，支持以下功能：

### 代码块
```python
def hello():
    print("Hello World")
```

### 提示框
!!! note "笔记"
    这是一个笔记框

### 表格
| 列1 | 列2 |
|-----|-----|
| 内容1 | 内容2 |

### 数学公式
$E = mc^2$

更多详情见：https://squidfunk.github.io/mkdocs-material/

---

## ❓ 常见问题

### Q: 推送后多久才能看到更新？
**A:** 通常 1-3 分钟。查看 Actions 标签页了解构建进度。

### Q: 网站显示 404？
**A:** 
1. 检查 `mkdocs.yml` 中的路径是否正确
2. 确认 markdown 文件确实存在
3. 等待 1-2 分钟后刷新浏览器

### Q: 如何回滚到之前的版本？
**A:** 使用 git 撤销：
```powershell
git revert <commit-hash>
git push origin main
```

### Q: 能否使用自定义域名？
**A:** 可以，编辑 `.github/workflows/deploy.yml`，修改 `cname` 字段。

---

## 💡 最佳实践

1. ✅ 定期在本地测试：`mkdocs serve`
2. ✅ 提交前检查拼写和语法
3. ✅ 使用有意义的 commit 信息
4. ✅ 不要修改 `site/` 目录（自动生成）
5. ✅ 保持 `main` 分支的整洁，不要混入生成文件

---

## 📞 需要帮助？

- 查看 MkDocs 文档：https://www.mkdocs.org/
- 查看 Material 主题：https://squidfunk.github.io/mkdocs-material/
- GitHub Actions 文档：https://docs.github.com/en/actions

---

**Happy Writing! 🎉**
